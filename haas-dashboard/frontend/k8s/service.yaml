apiVersion: v1
kind: Service
metadata:
  name: haas-frontend
  namespace: haas
spec:
  selector:
    app: haas-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
