apiVersion: v1
kind: Service
metadata:
  name: haas-backend
  namespace: haas
spec:
  selector:
    app: haas-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
